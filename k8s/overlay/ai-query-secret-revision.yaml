apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai
  namespace: openat
spec:
  template:
    metadata:
      annotations:
        # deploy/state 전용 값. CD가 ai-query-secrets resourceVersion으로 갱신한다.
        openat.io/ai-query-secret-revision: "bootstrap"
