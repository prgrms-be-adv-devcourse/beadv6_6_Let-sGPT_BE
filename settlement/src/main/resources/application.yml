server:
  port: 9140

spring:
  profiles:
    active: local
  application:
    name: settlement-service
  jpa:
    properties:
      hibernate:
        default_schema: settlement # 스키마 분리
    hibernate:
      ddl-auto: validate
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method