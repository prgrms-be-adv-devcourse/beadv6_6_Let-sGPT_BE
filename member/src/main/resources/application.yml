server:
  port: 9100


spring:
  profiles:
    active: local
  application:
    name: member-service
  jpa:
    properties:
      hibernate:
        default_schema: member # 스키마 분리
    hibernate:
      ddl-auto: validate
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method