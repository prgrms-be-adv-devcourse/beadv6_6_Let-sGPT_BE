# 담당 도메인 결정 이력 (Decision Log)

> 현재 담당하고 있는 Product 도메인(향후 도메인 추가 가능)의 핵심 아키텍처와 기술적 결정 사항들을 기록해 두는 공간입니다.  
> 각 결정의 배경 및 판단 근거와 트레이드 오프를 작성함으로써 결정 이력 추적 및 향후 비효율적 결정이라는 판단이 섰을 시 피드백 작성을 통해 개발 사고 확장을 위한 목적으로 사용됩니다.

---

## 2026-06-19

### 1. 개발 문서 구조 분리 및 관리 이원화
* **맥락 및 배경 (Context):** 로컬 가이드와 가공된 기술 히스토리의 성격을 분리해 전체적인 문서 관리 효율을 높이려는 목적임.
* **결정 사항 (Decision):** 로컬 작업 지침(`CLAUDE.md`)과 프로젝트 개요(`docs/PROJECT.md`)의 역할을 분리하고, 도메인 아키텍처 결정 이력은 이 파일로 통합 관리하기로 함.
* **트레이드 오프 (Trade-off):** 개인 설정이 포함될 수 있는 가이드 파일은 로컬에서만 제외(`git exclude`) 처리하여 형상 관리를 깔끔하게 유지하되, 프로젝트의 방향성을 담은 결정 이력은 팀의 자산이므로 히스토리가 남도록 추적하는 방향을 선택함.

### 2. 엔티티 초안 - AI, 상세 검토 - 휴먼 체크 방식 설계
* **맥락 및 배경 (Context):** 초기 보일러플레이트성 스키마 작성에 드는 시간을 줄이고, 표준 구조 기반으로 설계 누락을 방지하여 생산성을 높이기 위함임.
* **결정 사항 (Decision):** 초기 도메인 모델링 단계에서 표준 스키마 구조에 부합하는 초안은 AI를 활용해 빠르게 도출하고, 이후 개별 엔티티를 직접 검증하며 수정하는 방식을 채택함.
* **트레이드 오프 (Trade-off):** 리소스를 아끼는 대신 AI의 제안을 맹목적으로 수용하지는 않음. 엔티티를 하나씩 직접 뜯어보며 비즈니스 요구사항에 맞게 상세 설계를 수정하고 평가하는 검증 단계를 필수로 거침으로써 시스템의 정합성을 확보함.

### 3. 카테고리 구조 설계: Enum 구조에서 RDB 테이블로 분리
* **맥락 및 배경 (Context):** 당장은 Enum이 편할 수 있지만, 향후 대/중/소분류 확장이나 카테고리별 유연한 수수료 정책이 도입될 수 있는 비즈니스 확장성을 고려함.
* **결정 사항 (Decision):** 상품 카테고리 관리를 애플리케이션 `Enum` 대신 별도의 데이터베이스 테이블(`categories`)로 분리하여 관리하기로 함.
* **트레이드 오프 (Trade-off):** 초기 개발 공수는 조금 더 들더라도, 코드 수정과 재배포 없이 데이터 변경만으로 카테고리 구조를 유연하게 바꿀 수 있는 장기적인 확장성을 챙기는 편이 유리할 수 있다고 판단함.

### 4. 테이블 네이밍 컨벤션 표준화: 엔티티 단수형 / 테이블 복수형
* **맥락 및 배경 (Context):** `order`, `user`, `drop` 같은 SQL 주요 예약어와의 충돌을 원천 차단하고 데이터베이스 전반의 일관된 명명 규칙을 유지하기 위함임.
* **결정 사항 (Decision):** JPA 엔티티는 객체 인스턴스로 단수, 매핑되는 DB 테이블명은 복수형(예: `orders`, `users`)을 사용하기로 함.
* **트레이드 오프 (Trade-off):** 공통 DB를 사용하기 때문에 팀 일관성을 위해 합의를 도출해야 하는 비용이 있음. 이유 및 근거와 대안의 장단점을 팀에 공유하고 찬/반 방식으로 전원 찬성을 유도하여 팀 컨벤션으로 승격함.

### 5. 도메인 생성 맥락 명시를 위한 의미론적 빌더(Semantic Builder) 적용
* **맥락 및 배경 (Context):** String이나 Instant처럼 같은 타입의 파라미터가 여러 개 존재할 때, 호출부에서 인자 순서가 뒤바뀌어 발생하는 런타임 오류를 컴파일 시점에 막기 위함임.
* **결정 사항 (Decision):** 엔티티 생성 시 일반적인 정적 팩토리 메서드 대신, 행위의 의도를 담은 명명 기반의 빌더 패턴을 적용함. (예: `Product.create()...build()`, `Drop.schedule()...build()`)
* **트레이드 오프 (Trade-off):** 빌더를 반환하는 구조가 관례와 다를 수 있으나, 객체 생성 코드에 비즈니스 맥락을 명시하여 가독성과 안정성을 높이는 편이 구조적으로 유리하다고 판단함.

### 6. 인덱스 도입 결정
* **맥락 및 배경 (Context):** PostgreSQL은 MySQL과 달리 FK 참조 컬럼을 자동으로 인덱싱하지 않음. 인덱스가 없으면 부모 행이 수정·삭제될 때마다 자식 테이블 전체를 풀 스캔하게 되므로 성능 저하를 막고 데이터 정합성을 보장하기 위한 방어 가드가 필요했음.
* **결정 사항 (Decision):** 주요 조회 조건이 되는 컬럼뿐만 아니라 타 도메인을 참조하는 FK 및 타 도메인 값 참조 컬럼에 명시적으로 인덱스를 부여하기로 함.
* **트레이드 오프 (Trade-off):** 과도한 인덱스로 인한 쓰기 성능 저하를 방지하기 위해 우선 단일 인덱스로 시작하고, 정렬·페이징 조건이 고정되는 시점에 복합 인덱스로 최적화하여 오버스펙 고려 필요.

### 7. 동시성 제어 전략 결정
* **맥락 및 배경 (Context):** 한정 수량 판매(Drop) 구조에서 선착순 재고 차감을 위한 경쟁/동시성을 위한 효율적 전략 결정이 필요하였음.
* **결정 사항 (Decision):** Redis의 캐시 구조 도입으로 Drop 상태 변경/조회 관리는 캐시가 관리, 재고 변경 시 이력에 Append 방식으로 재고 경쟁 상황을 회피하고, 요청 트랜잭션을 원자적으로 처리하기 위해 Redis 내의 Lua 스크립트를 사용하기로 결정
* **트레이드 오프 (Trade-off):** 캐시 사용으로 인한 메모리를 차지할 수 있음, 그러니 비즈니스 특성상 Open 상태의 Drop 객체 개수는 한정적이므로 메모리 사용량은 미미하다 판단하여 사용 결정. (Drop 1000ea/2MB 이하 예상)

---

## 2026-06-22

### 1. 재고 이력 멱등성을 DB 복합 유니크 제약으로 보장
* **맥락 및 배경 (Context):** 재시도 및 중복 요청 시 동일 주문이 재고를 중복 차감하거나 복구하는 문제를 방지하고, 애플리케이션 레벨의 체크만으로는 방어하기 어려운 동시성 레이스 컨디션을 원천 차단하기 위함임.
* **결정 사항 (Decision):** 재고 이력 테이블(`stock_histories`)에 `UNIQUE(order_id, change_type)` 복합 제약을 부여하여, 동일 주문 내에서 동일 행위(DEDUCT/ROLLBACK)가 중복 INSERT되는 것을 막고 주문당 차감 1건, 롤백 1건의 멱등성을 보장하기로 함.
* **트레이드 오프 (Trade-off):** 멱등성 최종 방어선을 DB 제약에 위임하여 애플리케이션 내 유효성 검증 분기를 단순화함. 다만, 제약 위반 발생 시 이미 선점된 Redis 재고를 복구하는 보상 처리 흐름이 필요하므로, 해당 예외 처리 로직을 차감 프로세스에 함께 구현하는 방향을 선택함.

### 2. 상품 도메인 내부를 관리 단위별 서브도메인 패키지로 분리
* **맥락 및 배경 (Context):** 상품 도메인이 상품·카테고리·재고 등 여러 관리 단위를 품게 되면서, 레이어 우선 패키징으로는 한 디렉터리에 전부 쌓여 응집도가 떨어지고, 특히 재고 게이트키퍼처럼 비대해질 영역의 경계가 흐려지는 문제가 예상되었음.
* **결정 사항 (Decision):** 상품 모듈 내부를 관리 단위별 서브도메인 패키지(`product`/`category`/`drop`)로 분리하고, 각 서브도메인 안에서 도메인·애플리케이션·인프라·프레젠테이션 레이어를 유지하기로 함. Category도 별도 테이블로 분리해 관리 포인트로 명시한 만큼 독립 서브도메인으로 승격. 서브도메인 간 호출은 application 포트를 이용.
* **트레이드 오프 (Trade-off):** 코드가 적은 현재는 패키지 수가 다소 늘어나는 부담이 있으나, 구조 변경 비용이 가장 싼 초기에 경계를 잡아두는 편이 장기적으로 유리하다 판단함.

### 3. 상품 모듈 전용 가이드 문서(PRODUCT.md) 분리
* **맥락 및 배경 (Context):** 전역 문서(`PROJECT.md`)와 별개로, 상품 모듈 내부에서만 닫히는 구조·컨벤션(평탄 서브도메인 구성, 서브도메인 간 포트 호출 규칙, 빌더·DTO·에러코드 컨벤션 등)이 늘어나면서 한곳에 모아 둘 필요가 생겼음.
* **결정 사항 (Decision):** `product/docs/PRODUCT.md`를 만들어 모듈 내부 구조·컨벤션을 정리하고, 전역 규칙은 `PROJECT.md`를 참조하도록 역할을 분리(전역=`PROJECT.md` / 모듈 내부=`PRODUCT.md` / 결정 근거=`DECISIONS.md` / 재고 상세=`STOCK_GATEKEEPER.md`).
* **트레이드 오프 (Trade-off):** 관리할 문서가 하나 늘어 동기화 부담이 생기나, 전역 내용은 중복 작성 대신 `PROJECT.md` 링크로 참조해 드리프트를 줄이고, 담당 도메인 컨벤션을 한눈에 찾을 수 있는 이점을 택함.

### 4. 서비스 계층 CQRS 경량 적용(Query/Command 분리)
* **맥락 및 배경 (Context):** 비즈니스 밸리데이션 및 상태 변경을 담당하는 CUD 로직과 단순 데이터 프로젝션 위주의 R 로직이 혼재되어 발생하는 응집도 저하를 막고, 읽기 전용 트랜잭션의 `readOnly` 최적화를 명확히 적용하여 의도치 않은 상태 변경(Side Effect)을 방지하기 위함임.
* **결정 사항 (Decision):** 애플리케이션 포트와 서비스를 읽기(`XxxQueryService`)와 쓰기(`XxxCommandService`) 계층으로 명확히 분리하고, DTO 명명 규칙(`~Command` / `~Query` / `~Info`)도 분리하여 적용함.
* **트레이드 오프 (Trade-off):** 파일 및 클래스 개수가 증가하는 보일러플레이트 부담이 있으나, 이는 AI 에이전트 활용을 통해 생산성 손실을 쉽게 상쇄할 수 있음. 오히려 구조가 명확히 분리됨으로써 AI가 소스코드의 맥락을 오판하지 않고 더 정확하게 제어할 수 있는 이점을 택함. (데이터 저장소까지 분리하는 풀 CQRS는 현 단계에서 오버스펙이므로 제외)

### 5. Swagger 문서 구현 인터페이스 방식으로 결정
* **맥락 및 배경 (Context):** 팀 합의로 Swagger 도입은 확정되었으나 세부 구현 방식 결정이 필요하였으며, Swagger 어노테이션을 컨트롤러에 매핑하는 방식은 API가 많아질 시 가독성 저하가 우려됨
* **결정 사항 (Decision):** 명세 전담 인터페이스(`ProductApiSpec`)에 Swagger 관련 어노테이션을 몰아두고, 컨트롤러가 이를 `implements`하여 순수 제어 흐름만 남김. 단, 데이터 모델용 `@Schema`는 구조적 응집도를 위해 DTO record에 매핑함.
* **트레이드 오프 (Trade-off):** 메서드 시그니처를 두 곳에 유지하는 번거로움이 있으나, 컴파일 단계에서 정합성이 강제되어 명세 불일치(Drift) 위험이 없음. 상품 도메인은 향후 엔드포인트가 계속 늘어날 예정이므로 분리하는 편이 장기적으로 유리하다고 판단함.

### 6. 클라이언트 요청 ID 내부 추출 방식으로 변경
* **맥락 및 배경 (Context):** 각 클라이언트(프론트)에서 API 요청 시 구매자 id, 판매자 id 등 요청 사용자 식별 키는 JWT 인증 구조 특성상 변조에 취약함을 인지함.
* **결정 사항 (Decision):** 요청 사용자 id는 클라이언트에서 전송되는 것이 아닌 내부 JWT 인증 헤더 정보를 사용하는 것으로 변경
* **트레이드 오프 (Trade-off):** 보안 문제라 팀 컨벤션 차원에서 변경해야 하므로 각 도메인의 API 코드를 변경해야하는 트레이드 오프가 있으나, 보안 관련 부분이므로 타협할 수 없다고 생각하며, 팀에게 의견 공유 후 충돌 없이 자연스러운 합의로 변경 진행

---

## 2026-06-23

### 1. ApiResponse 제거
* **맥락 및 배경 (Context):** ApiResponse가 사실상 data 필드만 존재하여 봉투로써의 가치가 없음을 인지(상태 코드는 HTTP status 필드와 중복), 이 경우 ResponseEntity 응답 표준 클래스를 사용하는게 유리하다고 판단함, 팀원들과 의논시 당장 공통 응답 필드 추가 예정이 없어 제거하는게 유리해보임
* **결정 사항 (Decision):** ApiResponse 제거 후 컨트롤러 응답을 ResponseEntity를 사용하도록 변경
* **트레이드 오프 (Trade-off):** API 응답 컨벤션을 변경해야하는 비용이 들어가나, API 개수와 향후 사용할 표준을 고려했을 때 지금 시점에 변경하는 것이 유리하다고 판단하였으며, 팀원의 합의를 이끌어냄.

### 2. Product 도메인 내부 서브도메인 간 결합 전략 결정
* **맥락 및 배경 (Context):** Product 내 서브도메인 간 결합 방식에 따라 내부 통신 방식(이벤트 or 직접 호출) 및 엔티티 제약 조건(DB 레벨 or 코드 레벨 컨트롤) 설정으로 개발 효율성에 차이가 날 수 있어 결합 전략 결정.
* **결정 사항 (Decision):** 엔티티 결합 방식은 코드 레벨에서는 엔티티간 JPA 연관 참조 방식 사용, DB 레벨에서는 공용 DB 사용으로 DB 레벨 제약을 걸어 개발 효율성 및 정합성을 둘 다 가져가는 방식으로 결정하고, 도메인 간 호출은 Application 포트를 이용하는 것으로 결정
* **트레이드 오프 (Trade-off):** 코드레벨에서는 JPA 연관 관계, DB 레벨에서는 DB 참조 규약으로 묶음에 따라 서브 도메인 엔티티간 강한 결합이 생김. 하지만 서브 도메인 단위에서는 엔티티간 결합은 가져가 개발 효율성을 높이고, 메인 도메인으로 승격 시 코드 레벨에서의 빠른 분리를 위해 Application 포트를 참조하는 방식으로 결정함.

### 3. 서브도메인별 삭제 전략 결정
* **맥락 및 배경 (Context):** 서브 도메인별 비즈니스 요구가 다르기 때문에 각 서브도메인 별 효율적인 삭제 전략 결정 필요
* **결정 사항 (Decision):** `category`는 하드 삭제 + 자식 `product`의 참조 category key null(카테고리 없음) 설정, `product` 는 소프트 삭제(deletedAt 설정) + 동일 시점에 자식 `drop`도 소프트 삭제(재고 이력 및 한정판 플랫폼 특성상 drop 재고 복구 가능성 있는 게 합리적이라고 판단)
* **트레이드 오프 (Trade-off):** 부모 삭제 시점에 자식을 1:N으로 삭제하여 삭제 이벤트가 무거워지나, 한정판 플랫폼 특성상 충분히 감당 가능한 개수(상품 수량 or drop 수량 개수 < 1000)로 예상하여 상기 방법으로 결정

---

## 2026-06-24

### 1. 담당 도메인 테스트 전략 결정
* **맥락 및 배경 (Context):** 담당 도메인의 테스트 일관성과 정합성 검증 기준을 세우기 위해 테스트 전략 결정이 필요했음.
* **결정 사항 (Decision):** 초기에는 API 경계 단위(클라이언트 요청·내부 Kafka·OpenFeign 호출 단위)로 테스트하는 방식을 고려했으나, Drop의 재고 정합성·요청 처리 판정처럼 경계 안쪽에서 정확히 검증해야 하는 로직이 있어 클린 아키텍처 계층 경계별로 최적의 도구를 선정하는 방식으로 결정함. 경계별 도구와 상세 작성 지침은 `product/docs/TEST_CONVENTION.md`로 분리해 관리하도록 함.
* **트레이드 오프 (Trade-off):** 프로젝트 규모 대비 과한 테스트로 비칠 수 있으나, 상세 지침을 갖춰 두면 AI 에이전트로 테스트 코드 작성 시간을 크게 줄일 수 있고, 구현과 테스트의 지침·세션을 독립적으로 분리해 작성-검증 효율을 극대화할 수 있다고 판단함.

### 2. QueryDSL 도입 결정
* **맥락 및 배경 (Context):** 상품 목록 조회 기능 작성 시 동적 필터링 기능 필요 및 N + 1 가능성 문제를 인지하였으며 한정판 드롭 커머스의 비즈니스 특성상 일시에 상품 목록 조회가 몰릴 수 있어 효율적인 쿼리 탐색 구조가 필요하였음.
* **결정 사항 (Decision):** 현재 도메인 기능 초기이지만 DROP 도메인 구현 시 복잡한 동적 쿼리 기능이 확정인 상태이므로 구조적 일관성을 위해 QueryDSL 도입을 통해 상품 목록 조회(구매자 상품 탐색)부터 적용하기로 결정, 향후 처리량 테스트하여 캐시 도입도 고려해야 함.
* **트레이드 오프 (Trade-off):** 비교적 적은 필터링에 QueryDSL 도입은 오버엔지니어링일 수 있음, 비교적 간단한 @EntityGraph 도입부터 고려하였으나 곧 복잡한 쿼리 처리를 위한 기준을 따로 마련해야 하므로 총소유비용 관점에서 현 단계에 도입하는 것이 합리적이라고 판단함.

---

## 2026-06-25

### 1. 판매자 클라이언트 소유 구조 결정
* **맥락 및 배경 (Context):** 회원과 판매자 관계가 1:N 방식이므로 클라이언트의 회원 - 판매자 소유 방식 결정에 따라 API 요청 방식이 변경될 수 있으므로 소유 방식 결정이 필요함
* **결정 사항 (Decision):** 사용자 로그인 이후 N개의 판매자 중 사용할 판매자를 선택하여 클라이언트 세션에 선택 판매자를 보관하는 방식으로 결정
* **트레이드 오프 (Trade-off):** 판매자의 동시 작업이 제한될 수 있으나 한정판 비즈니스 특성상 판매 기능이 순차적으로 작업되어도 무리가 없다고 판단하여 결정

### 2. 회원 - 판매자 소유 검증 방식 결정
* **맥락 및 배경 (Context):** 2026-06-22 문서에 판매자 위변조 검증이 필요하다고 결정하였으며, 관련하여 실제 구체적 검증 방식 결정이 필요했음.
* **결정 사항 (Decision):** 회원 - 판매자 검증은 회원 도메인 책임과 연동되므로 상기 1번의 결정에 따라 회원 도메인 담당자와 미팅 후 클라이언트 요청에 대해 회원 - 판매자 소유 여부를 한번 더 내부 API 방식으로 검증하는 방식으로 결정
* **트레이드 오프 (Trade-off):** 요청 시 회원 소유자 검증에 대한 트래픽이 생기나, openFeign 방식으로 비교적 부하가 적을 수 있고, 현 단계에서 JWT에 sellerId를 추가하면 오버엔지니어링이 될 수 있다고 판단하여 향후 테스트 후 부하에 따라 변경하는 것으로 결정

### 3. 회원 - 판매자 소유 검증 방식 변경
* **맥락 및 배경 (Context):** 미팅 후 결정되었던 2번 문서에 대해 내부 API 검증 방식에서 회원 도메인 담당자가 결합도 측면에서 불리할 것 같다고 방식 변경 요청하였음
* **결정 사항 (Decision):** 내부 검증 방식이 아닌 게이트웨이 신뢰 방식으로 변경, 클라이언트가 로그인 상태에서 판매자를 선택하면 토큰 재발급 요청 방식. 토큰은 기존 회원과 독립적인 판매자 회원 방식으로 가겠다고 회원 담당자가 결정 및 전달
* **트레이드 오프 (Trade-off):** 이미 결정된 사항에 대해 문서 작성 및 PR 생성 및 병합까지 진행된 매몰비용 발생, 다만 문서 자체가 추상적인 내용 기준으로 작성하였고(구현은 도메인 책임), 회원 도메인이 구현 완료 시 확인 후 연동하겠다고 결정하여 상대적으로 매몰비용은 낮음. 그러나 향후 결정 방식 및 문서 확정 작성 방식에 대한 고민은 필요할 것으로 보임.

### 4. 드롭 처리에 대한 철학 정의
* **맥락 및 배경 (Context):** 드롭 처리 방식(철학)에 대한 명확한 기준을 정의하여 구현 시 충돌 사항에 대해 하나의 기준으로 명확한 결정을 하기 위한 기준 정의
* **결정 사항 (Decision):** 초안에서 Redis를 도입하기로 결정하였으므로, 드롭에 대한 라이브 조회, 변경 처리는 캐시(Redis)를 이용, Drop 테이블은 영구적 반영 사항만 기록하는 테이블로 정의함. 재고 이력은 유일한 진실성을 유지하기 위해 RDB append 방식으로 정의 
* **트레이드 오프 (Trade-off):** Drop의 캐시 의존성이 높아지고 메모리를 사용하지만 캐시, RDB의 역할을 명확히 정의함으로써 속도와 정합성을 모두 챙겨 한정판 대용량 처리에서 높은 가치를 보일 수 있는 구조라고 생각함.   

### 5. 드롭 오픈 도래 시 캐시 적재 방법 결정
* **맥락 및 배경 (Context):** 드롭 초안을 바탕으로 드롭 상세 설계 진행 중 드롭 오픈 시 캐시에 적재하는 시기 결정 필요, 레디스 캐시 효율성을 고려하여 모든 드롭을 올리는 방식은 초안에서 배제하고 오픈된 드롭만 관리하는 방식으로 설계하였기 때문에 오픈이 도래한 드롭 적재 방법에 따라 오픈 시점의 부하 효율성이 결정됨
* **결정 사항 (Decision):** TaskScheduler 사용(오픈 5분 전 적재)으로 결정함. 콜드 부팅 시 인메모리 초기화 시점을 레디스 재고 집계와 동일한 시점 기준으로 가져갈 수 있고 스케줄링은 JVM에게 위임하는 게 효율적이라 판단함. 쿼츠 스케줄러나 스프링 스케줄러 방식도 고려하였으나, 스프링 스케줄러는 예정이 없을 때에도 상시 주기로 폴링는 한다는 점에서 비효율로 다가왔고, 쿼츠 스케줄러 방식은 기능 구현에 추가 DB가 많이 생성될 수 있어 선택에서 탈락함 
* **트레이드 오프 (Trade-off):** 메모리 부담이 증가할 수 있으나 시나리오 초안에서는 한정판 오픈 예정 및 오픈 동시 개수가 최대 1000개 정도로 메모리 부담이 낮고(~2MB 예상) 분산 DB에서는 중복 등록될 수 있음. 하지만 분산 인스턴스 환경 도입 시점에 ZSET를 이용한 락 관리 방식 등을 필요 시 도입하면 될 것으로 보임.

### 6. 재고 차감 성공 응답 시점 결정
* **맥락 및 배경 (Context):** 현재 Redis 캐시를 도입하기로 하였으므로 재고 차감 응답 시점이 캐시 내 차감 응답 or 캐시 차감 후 db insert 이후 응답으로 분기가 생겨 결정 필요
* **결정 사항 (Decision):** 초안 설계에서 필드 경쟁을 배제하고 재고 이력을 append를 설계한 방식 자체가 응답 속도 향상과 정합성 유지였기 때문에 insert 이후 응답으로 결정함  
* **트레이드 오프 (Trade-off):** 캐시에서 즉시 응답 대비 미세한 응답 지연이 있을 수 있으나 정합성 및 오류 발생 시 복구 차원에서 비교할 수 없는 리스크 차이가 있으므로 insert 이후(재고 차감 확정) 응답으로 결정함

### 7. 1인 구매 한도 판정 방식 결정
* **맥락 및 배경 (Context):** 사용자가 1인 구매 한도를 초과하여 구매하는 것을 방지하기 위한 방법 결정 필요
* **결정 사항 (Decision):** Redis 내부에서 사용자별 구매 집계 카운터로 사용하는 방식으로 결정, 캐시 워밍 시점에는 재고 이력 집계와 동시 연산 처리 진행
* **트레이드 오프 (Trade-off):** 미세한 메모리 증가가 있을 수 있으나(비즈니스적으로 무의미할 것으로 보임) DROP 관리에 캐시를 도입한 시점에서 건별 RDB 조회 방식 대비 이득이 압도적이라 판단함.

### 8. 드롭 종료 케이스 결정
* **맥락 및 배경 (Context):** 드롭 종료 케이스에 따라 구현이 바뀌므로 비즈니스 특성을 고려하여 드롭 CLOSE(종료) 케이스 결정 필요
* **결정 사항 (Decision):** 드롭 종료 케이스는 1. 사용자 직접 드롭 취소 2. 드롭 종료 예약 두 케이스로 결정. 매진(재고 0)은 환불, 반품 등 가역적 상태이므로 드롭 종료 케이스로 보지 않음.   
* **트레이드 오프 (Trade-off):** 드롭 종료 예약은 확실한 비즈니스 요구가 없을 수 있음. 하지만 한정판이라는 플랫폼 특성상 수량을 가치로 보는 한정과 더불어 기간에 대한 한정 가치를 더할 수 있으므로, 기술적 구현 난이도가 거의 동일(시작과 종료 동일 TaskSchedule 사용)하여 한정판 예약 종료 케이스를 넣었음.    

### 9. 드롭 상태 변경
* **맥락 및 배경 (Context):** 드롭 상태를 초안에는 직관적인 SCHEDULED, OPEN, CLOSE, SOLD_OUT 4단계로 설계하였으나 구현 단계에서 스케줄 설정 및 시간과 상태의 동기화 문제가 발생하여 해결이 필요하였음
* **결정 사항 (Decision):** 상태를 오직 REGISTERED, CLOSE 두 상태만 가져가고 캐시에서 오픈 시간과 재고 개수 두 가지만 가지고 OPEN, SOLD_OUT을 판단하기로 결정함.
* **트레이드 오프 (Trade-off):** 상태만 반환하는 것보다 계산이 들어가므로 연산 처리가 추가되나 연산량이 극도로 미미하고 캐시에서 처리하므로 동기화 시점 등 처리 일관성 측면에서 파생 상태 방식의 판단이 압도적으로 유리하다고 판단함.

## 2026-06-26

### 1. 드롭 수정 및 취소 전략 결정
* **맥락 및 배경 (Context):** 드롭은 한정판 특성상 오픈 이전, 이후 수정 삭제가 비즈니스적으로 단순하지 않기 때문에 수정 및 삭제 전략을 결정해야 함
* **결정 사항 (Decision):** 수정 기능은 제외하고 수정 필요 시 삭제 -> 재등록으로 정의(오픈 후에는 수정 개념X CLOSE만 가능), 삭제 전략은 오픈 전: soft delete, 오픈 후: 상태 -> CLOSE로 결정. 오픈 전은 하드 삭제도 고려하였으나, 상품 삭제 전파가 @SoftDelete로 전파되어 케이스가 분기되고 삭제 로직이 복잡해지는 리스크가 존재하여 제외함.   
* **트레이드 오프 (Trade-off):** 오픈 후 CLOSE는 주문 또는 결제 도중(재고 선점 시점)에 CLOSE되어 즉시 막을 경우 잘못하면 정합성에 큰 오류가 생길 수 있음. 때문에 한정판 도메인에서는 진행 도중 CLOSE는 실패처리 하지 않고, 새로 받는 요청만 취소 처리하도록 하고, 상세한 취소 컷은 사가 오케스트레이션인 주문 도메인 담당자에게 전달하여 결정하도록 함.

### 2. 상품 삭제 파생 전략 결정
* **맥락 및 배경 (Context):** Drop 구현 전까지 TODO로 보류중이던 상품 삭제 시 자식 드롭들의 처리 방법이 Drop 구현 완료로 확정이 필요하며, 2026-06-23의 #3 결정에서는 Product 삭제 시 Drop의 Soft 삭제를 명시하였으나 이는 Drop의 오픈 전 케이스 기준이므로 오픈 이후의 구체적 삭제 전략 결정이 필요함 
* **결정 사항 (Decision):** Drop 오픈 이후의 취소는 상태를 close로 바꾸는 것으로 확정하였으나, 오픈 이후 부모 상품의 삭제는 플렛폼 특성상 근본적으로 불가해야하므로 자식 Drop 중 open 상태가 있으면 삭제 거절, 없으면 soft 삭제로 결정함  
* **트레이드 오프 (Trade-off):** 결정 이력에서 서브도메인 간 결합 전략에 의해 역방향 참조는 금지해야 하므로 `Product` -> `Drop`의 직접 참조 대신 이벤트 방식으로 구현 진행  